(function($) {

		$(document).foundation();

		$('.style-name-here').slick({
      autoplay: true,
      autoplaySpeed: 2000,
      dots: true,
      infinite: true,
      speed: 500,
   });

})(jQuery);
